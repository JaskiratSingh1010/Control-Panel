# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
.venv/
env/
.env.bak

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
media/

# Secrets — never commit
.env
*.bat
# IDEs
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Claude Code session data — never commit
.claude/
server.err.log
# temp preview/harness files
_*.html
_*.png

# Business data workbooks — never commit (data, not code)
Excel File/
*.xlsx
*.xls
~$*

# Exported Claude Code chat history (large, local only)
claude-chat-history.md

# Local cache directory (file-backed Django cache)
.django-cache/

# TLS material for the LAN/HTTPS server. The private key must never be committed -
# anyone holding it can impersonate the panel. Generate per machine with
# deploy/make_cert.sh; the .crt is handed to users to trust, the .key stays here.
certs/
